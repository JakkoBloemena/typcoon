# Funnel readings

Board-visible funnel numbers. Source of truth until a read-only token lands
(decisions/008): the Shareholder pastes the latest Telegram 08:00 digest here from
the /ceo channel — weekly is enough. Once `FUNNEL_READ_TOKEN` is provisioned
(decisions/008, Shareholder ask 2), the monitor appends a row per tick instead.

Numbers here are real readings only — never estimated, never backfilled from memory.
An empty table is a gap the monitor reports, not a blank to fill in.

| Date (digest day) | Pageviews | Game-starts | Engaged (≥2 sessions) | Parent opt-ins | Total accounts | Source |
|---|---|---|---|---|---|---|

## Quota notes (optional, decisions/008 ask 3)

Free-tier consumption observations (Vercel/Supabase usage pages), when the
Shareholder has looked. One line per reading: date, service, metric, % of cap.
